# Core dependencies for reproducing the paper figures
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
openpyxl>=3.1          # reading the .xlsx model output files

# Optional: only needed for the country map figure (create_country_map.py),
# which also requires the Natural Earth subunits shapefile (see scripts note in README).
geopandas>=0.13
